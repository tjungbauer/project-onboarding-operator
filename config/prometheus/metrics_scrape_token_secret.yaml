# Token for Prometheus ServiceMonitor scraping (OpenShift user-workload monitoring rejects bearerTokenFile).
apiVersion: v1
kind: Secret
metadata:
  name: controller-manager-metrics-reader-token
  namespace: system
  annotations:
    kubernetes.io/service-account.name: controller-manager
type: kubernetes.io/service-account-token
