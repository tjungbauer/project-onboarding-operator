## Append samples of your project ##
# ProjectOnboarding and TShirtSize are cluster-scoped; spec.namespaces[].name is the tenant namespace.
# v1alpha1 manifests for conversion testing live under conversion/ (not bundled in OLM alm-examples).

resources:
- onboarding_v1beta1_tshirtsizes_catalog.yaml
- onboarding_v1beta1_projectonboarding.yaml
- onboarding_v1beta1_projectonboarding_tshirt.yaml
- onboarding_v1beta1_projectonboarding_gitops.yaml
# Optional cluster defaults (apply to operator namespace): onboarding_defaults_configmap.yaml
# Migration examples from helper-proj-onboarding tenants: migration/
